<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="60dp"
    android:paddingEnd="8dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <TextView
        android:id="@+id/text_message_body"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:background="@drawable/red_rect"
        android:padding="10dp"
        android:elevation="1dp"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:text="This is a sent message!"
        android:maxWidth="280dp" />

    <TextView
        android:id="@+id/text_message_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/text_message_body"
        android:layout_toStartOf="@id/text_edit_indicator"
        android:layout_marginEnd="4dp"
        android:layout_marginTop="2dp"
        android:textSize="12sp"
        android:textColor="@color/Gray"
        android:text="4:49 PM"/>

    <!-- NEW: Edit Indicator -->
    <TextView
        android:id="@+id/text_edit_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/text_message_body"
        android:layout_alignEnd="@id/text_message_body"
        android:layout_marginTop="2dp"
        android:text="Edited"
        android:textSize="12sp"
        android:textColor="@color/Gray"
        android:visibility="gone" />

</RelativeLayout>
